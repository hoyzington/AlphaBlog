<h1>Showing article details</h1>

<p><strong>Title: </strong><%= @article.title %></p>
<p><strong>Description: </strong><%= @article.description %></p>

<%= link_to 'Edit', edit_article_path(a), class: "btn btn-blue" %>
<%= link_to 'Delete', article_path(a), method: :delete, data: {confirm: 'Are you sure?'}, class: "btn btn-red" %>
<%= link_to 'Return to articles listing', articles_path, class: "btn btn-blue" %>
