<div class="card card-articles text-center mt-3 mb-4 justify-content-md-center">
  <div class="card-header pb-1 pt-3">
    <h2 class="mb-1"><%= link_to article.title, article, class: "link-white title" %></h2>
    <h6><%= time_ago_in_words(article.created_at) %> ago</h6>
    <h3><%= render article.categories %></h3>
  </div>
  <div class="card-body text">
    <h6 class="card-title font-italic">by <%= link_to (gravatar_for @user, size: 35), @user %> <%= link_to @user.username, @user, class: "link-blue" %></h6>
    <p class="card-text"><%= truncate(article.content, length: 370) %></p>
  </div> 
  <div class="card-footer">
  </div>
</div>
